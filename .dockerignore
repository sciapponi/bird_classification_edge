# Git
.git
.gitignore

# Docker
.dockerignore
Dockerfile
docker-compose.yml
run_docker_training.sh
run_docker_benchmark.sh
run_docker_soft_labels.sh
run_docker_distillation.sh

# Python virtual environments
venv
.venv
birdenv
*.pyc
*.pyo
__pycache__

# Datasets (these are mounted, not copied into the image)
bird_sound_dataset/
ESC-50-master/
augmented_dataset/

# Log files (these are mounted)
logs/

# Benchmark results (these are generated at runtime)
benchmark/benchmark_results/

# IDE and OS specific files
.idea/
.vscode/
*.DS_Store
Thumbs.db

# Markdown files (if very large and not needed in the image)
*.md

# Large model files (these are mounted)
*.pt
*.pth

# Other output or temporary files
soft_labels_complete/
test_soft_labels/
misc/
table/
images/
present.pdf
*.pdf

# Additional exclusions for cleaner builds
.pytest_cache/
.coverage
htmlcov/
dist/
build/
*.egg-info/ 