
0) nel benchmark ancora predice troppo birdnet la classe "no_birds". capire perchè e ricorardselo -> risolto facendo predire solo le classi senza no_birds. si può provare anche a togliere la soglia del tutto e birdnet predicel'uccello più probabile e basta
0.5) cosine annealing learning rate scheduler
1.5) fare benchmark rispetto a bird-net dell'ultimo modello con focal loss -> fatto (senza classe no-bird)
2) Portare tutto il dataset in un server (con o senza preprocessing già fatto) -> scritto script di analisi 
2.25) Estrarre i soft labels per il dataset completo e fare distillation 
2.5) Monitorare meglio il training time capire dove fa bottleneck (nel preprocessing)
3) Fare gli esperimenti
    a) specie "facili"
    b) specie "difficili"
    c) specie divise per frequenze
3.5) confrontare mel, linear, il nostro e una versione fully learnable 
4) Altre modifiche strutturali? fare ablation studies su tutti questi cambiamenti
    - Apprendere le filter bank come vettori al posto che solo semi learnable (forse appesantisce ma ci sta provare per vedere se funziona bene). quale assumption è milgiore? L
    - HPSS? non so se ha senso, devo leggermi qualche paper a riguardo se hanno ottenuto prestazioni migliori
    - hierarchical loss? hierarchical combinata con focal loss? può essere pesata la hierarchical
